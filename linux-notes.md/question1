标题：为什么Linux里一切皆文件？

1. 什么是“一切皆文件”？

在Linux中，几乎所有资源都被抽象为文件：普通文件、目录、设备（硬盘、键盘、显示器）、进程间通信管道、网络套接字等。

这意味着你可以用相同的系统调用（如  open ,  read ,  write ,  close ）来操作它们。

2. 证据：观察  /dev  目录

执行  ls -la /dev ，你会看到很多设备文件，第一列的第一个字母表示类型：

 b  = block device（块设备，如硬盘  /dev/sda ）

 c  = character device（字符设备，如键盘、串口）

尝试  cat /dev/urandom  可以看到随机字节流（按 Ctrl+C 退出）。

3. 对比：普通文件 vs 设备文件

普通文件（如  /etc/passwd ）存储数据。

设备文件（如  /dev/null ）读写时会触发内核驱动，而不是真正的磁盘 I/O。

例如： echo "test" > /dev/null  会把数据扔进黑洞，没有任何存储。

4. 为什么这样设计？

统一接口：程序员只需学习一套文件操作 API，就能操作所有硬件和资源。

简化脚本：shell 命令可以轻松组合，例如  ls > output.txt  就是把目录列表写入文件。

5. 你自己的发现

通过  less /etc/passwd  你看到了什么？每一行的格式是怎样的？

尝试  ls -l /dev/stdin ，你会发现标准输入也是一个符号链接。

